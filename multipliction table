#include <iostream>

int main() {
    int limit = 10;

    cout << "\nMultiplication Table:\n";
    for (int i = 1; i <= limit; i++) {
        for (int j = 1; j <= limit; j++) {
            cout << i * j << "\t";
        }
        cout << "\n";
    }

    return 0;
}
