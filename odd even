#include <iostream>
using namespace std;

int main() {
    int num = 10; // replace this value with the desired integer number
    if (num % 2 == 0) {
        cout << num << " is an EVEN number." << endl;
    } else {
        cout << num << " is an ODD number." << endl;
    }
    return 0;
}
